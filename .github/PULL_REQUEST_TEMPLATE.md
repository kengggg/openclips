## Summary

## Validation
- [ ] `pytest` passes
- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] Live-tested against a camera (say which firmware) or marked as untested

## Protocol notes
If this changes what we send to the camera, link the evidence (capture, firmware string, docs update).
