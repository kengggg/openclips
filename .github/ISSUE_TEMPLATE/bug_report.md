---
name: Bug report
about: Something does not work with your camera or the library
labels: bug
---

**What happened**

**What you expected**

**Steps to reproduce**

```
openclips -v <command>
```

**Environment**
- OS / distribution and BlueZ version (`bluetoothctl --version`):
- Python version and how you installed openclips:
- Transport (`btgatt` / `bleak`):
- Camera firmware, if known (the `PRIVATE_QUERY` output or the original app showed it):

**Logs**
Paste the `-v` output. Redact your pairing key and Wi-Fi passphrase; a camera
BLE address is fine to leave in.
